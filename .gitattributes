# Normalize line endings: LF in the repo, checkout as-is per platform.
* text=auto eol=lf

# Binary/lockfiles should never be normalized.
package-lock.json -text
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
